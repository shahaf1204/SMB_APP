import {
  CircleCheck,
  Clock3,
  Info,
  Lightbulb,
  TriangleAlert,
  type LucideIcon,
} from 'lucide-react';
import type { BusinessInsightIcon } from '../../business-coach/types';

export const INSIGHT_ICON_MAP: Record<BusinessInsightIcon, LucideIcon> = {
  TriangleAlert,
  Lightbulb,
  Clock3,
  CircleCheck,
  Info,
};
